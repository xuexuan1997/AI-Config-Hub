name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: read

jobs:
  package:
    permissions:
      contents: read
    uses: ./.github/workflows/linux-package.yml

  publish:
    needs: package
    timeout-minutes: 15
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: linux-x64-release-candidate
          path: release/linux-x64
      - uses: actions/download-artifact@v4
        with:
          name: windows-x64-release-candidate
          path: release/windows-x64
      - uses: actions/download-artifact@v4
        with:
          name: macos-x64-release-candidate
          path: release/macos-x64
      - uses: actions/download-artifact@v4
        with:
          name: macos-arm64-release-candidate
          path: release/macos-arm64
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm release:verify release/linux-x64 release/windows-x64 release/macos-x64 release/macos-arm64
      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          source_assets=(
            release/linux-x64/AI-Config-Hub-*-x86_64.AppImage
            release/linux-x64/latest-linux.yml
            release/linux-x64/SHA256SUMS
            release/linux-x64/version-manifest.json
            release/linux-x64/sbom.cdx.json
            release/linux-x64/elf-compatibility.json
            release/windows-x64/AI-Config-Hub-*-windows-x64.exe
            release/windows-x64/AI-Config-Hub-*-windows-x64.exe.blockmap
            release/windows-x64/latest.yml
            release/windows-x64/SHA256SUMS
            release/windows-x64/version-manifest.json
            release/windows-x64/sbom.cdx.json
            release/macos-x64/AI-Config-Hub-*-macos-x64.dmg
            release/macos-x64/SHA256SUMS
            release/macos-x64/version-manifest.json
            release/macos-x64/sbom.cdx.json
            release/macos-arm64/AI-Config-Hub-*-macos-arm64.dmg
            release/macos-arm64/SHA256SUMS
            release/macos-arm64/version-manifest.json
            release/macos-arm64/sbom.cdx.json
          )
          for asset in "${source_assets[@]}"; do
            test -f "$asset"
          done

          publish_dir=release/publish-assets
          mkdir -p "$publish_dir"
          cp release/linux-x64/AI-Config-Hub-*-x86_64.AppImage "$publish_dir/"
          cp release/linux-x64/latest-linux.yml "$publish_dir/latest-linux.yml"
          cp release/linux-x64/SHA256SUMS "$publish_dir/linux-x64-SHA256SUMS"
          cp release/linux-x64/version-manifest.json "$publish_dir/linux-x64-version-manifest.json"
          cp release/linux-x64/sbom.cdx.json "$publish_dir/linux-x64-sbom.cdx.json"
          cp release/linux-x64/elf-compatibility.json "$publish_dir/linux-x64-elf-compatibility.json"
          cp release/windows-x64/AI-Config-Hub-*-windows-x64.exe "$publish_dir/"
          cp release/windows-x64/AI-Config-Hub-*-windows-x64.exe.blockmap "$publish_dir/"
          cp release/windows-x64/latest.yml "$publish_dir/latest.yml"
          cp release/windows-x64/SHA256SUMS "$publish_dir/windows-x64-SHA256SUMS"
          cp release/windows-x64/version-manifest.json "$publish_dir/windows-x64-version-manifest.json"
          cp release/windows-x64/sbom.cdx.json "$publish_dir/windows-x64-sbom.cdx.json"
          cp release/macos-x64/AI-Config-Hub-*-macos-x64.dmg "$publish_dir/"
          cp release/macos-x64/SHA256SUMS "$publish_dir/macos-x64-SHA256SUMS"
          cp release/macos-x64/version-manifest.json "$publish_dir/macos-x64-version-manifest.json"
          cp release/macos-x64/sbom.cdx.json "$publish_dir/macos-x64-sbom.cdx.json"
          cp release/macos-arm64/AI-Config-Hub-*-macos-arm64.dmg "$publish_dir/"
          cp release/macos-arm64/SHA256SUMS "$publish_dir/macos-arm64-SHA256SUMS"
          cp release/macos-arm64/version-manifest.json "$publish_dir/macos-arm64-version-manifest.json"
          cp release/macos-arm64/sbom.cdx.json "$publish_dir/macos-arm64-sbom.cdx.json"
          assets=("$publish_dir"/*)

          if gh release view "$GITHUB_REF_NAME" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            gh release upload "$GITHUB_REF_NAME" "${assets[@]}" --repo "$GITHUB_REPOSITORY" --clobber
          else
            gh release create "$GITHUB_REF_NAME" "${assets[@]}" \
              --repo "$GITHUB_REPOSITORY" \
              --title "$GITHUB_REF_NAME" \
              --generate-notes \
              --verify-tag
          fi
